{% extends 'layout/base_user.html.twig' %}

{% block body %}
    <div class="mx-auto col-10 col-sm-8 col-md-7 col-lg-5">
        <h1 class="text-primary text-center">Mon compte</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}

        <div class="text-center">
            <button type="submit" class="btn btn-warning rounded-pill">Enregistrer</button>
        </div>
        {{ form_end(form) }}
    </div>

{% endblock %}