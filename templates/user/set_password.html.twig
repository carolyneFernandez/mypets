{% extends 'base.html.twig' %}

{% block body %}
    <div class="mx-auto my-auto col-10 col-sm-8 col-md-5 col-lg-3 d-flex justify-content-center align-items-center div-login">
        {{ form_start(form, {'attr': {'class': 'w-100 mt-4 no-input-br'} }) }}
        <div class="text-center">
            <img src="{{ asset('assets/img/homepage/mypets_logo2.png') }}" alt="logo">
        </div>

        <h1 class="h3 mb-3 font-weight-normal text-center">Mot de passe</h1>

        {{ form_widget(form) }}

        <div class="d-flex justify-content-center">
            <button class="btn btn-success rounded-pill " type="submit">Enregistrer</button>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}