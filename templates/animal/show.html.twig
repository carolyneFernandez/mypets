{% extends 'layout/base_user.html.twig' %}

{% block title %}MyPets | Détails information animal{% endblock %}

{% block body %}
    <h1 class="text-center">Animal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ animal.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ animal.nom }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ animal.photo }}</td>
            </tr>
            <tr>
                <th>DateNaissance</th>
                <td>{{ animal.dateNaissance ? animal.dateNaissance|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Race</th>
                <td>{{ animal.race }}</td>
            </tr>
            <tr>
                <th>Puce</th>
                <td>{{ animal.puce }}</td>
            </tr>
            <tr>
                <th>InfosPere</th>
                <td>{{ animal.infosPere }}</td>
            </tr>
            <tr>
                <th>InfosMere</th>
                <td>{{ animal.infosMere }}</td>
            </tr>
            <tr>
                <th>Traitements</th>
                <td>{{ animal.traitements }}</td>
            </tr>
            <tr>
                <th>Decede</th>
                <td>{{ animal.decede ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>DateDeces</th>
                <td>{{ animal.dateDeces ? animal.dateDeces|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>CauseDeces</th>
                <td>{{ animal.causeDeces }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('animal_index') }}">back to list</a>

    <a href="{{ path('animal_edit', {'id': animal.id}) }}">edit</a>

    {{ include('animal/_delete_form.html.twig') }}
{% endblock %}
